# SkyPilot task — Bitcoin RNN smoke job (~15–30 min on T4)
# CarbonSight estimates: duration from --max-minutes in run:; pass --gpu-util 0.75 on advise/run.
name: carbonsight-bitcoin-rnn

workdir: .

resources:
  infra: aws
  accelerators: T4:1
  cpus: 4
  memory: 16

setup: |
  pip install -r requirements.txt

run: |
  python train_stub.py --epochs 120 --max-minutes 25 --require-cuda
